/*=============================================== InputCounter sub components types ===============================================*/

import type { ILibInputBaseMixin } from "../ComponentsMixins"

// ! DO NOT EXPORT THOSE TYPES, ONLY USE THEM IN INPUT_COUNTER COMPONENTS

export interface ILibInputCounterStyles extends ILibInputBaseMixin {
    $length: number
}
