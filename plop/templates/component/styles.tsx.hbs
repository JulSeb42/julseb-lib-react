

import styled from "styled-components"
import { setDefaultTheme } from "../../"
import type { } from "../../types"
import type { } from "./types"

const Styled{{ pascalCase name }} = styled.{{ lowerCase tag }}<{ }>`

`

setDefaultTheme([Styled{{ pascalCase name }}])

export { Styled{{ pascalCase name }} }
