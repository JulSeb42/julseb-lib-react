import { forwardRef } from "react"
import clsx from "clsx"
import {  } from "@julseb-lib/utils"
import type { ILib{{ pascalCase name }} } from "./types"

/** 
 * @description Returns a {{ pascalCase name }} component
 * @link https://documentation-components-react.vercel.app/{{ category }}/{{ kebabCase name }}
 * @extends HTML{{ pascalCase attribute }}Element
 * @prop element?: ElementType
 * @prop ref?: ForwardedRef<HTML{{ pascalCase attribute }}Element>
 */
export const {{ pascalCase name }} = forwardRef<HTML{{ pascalCase attribute }}Element, ILib{{ pascalCase name }}>(
    (
        { className, element = "{{ lowerCase tag }}", {{#if children}} children,{{/if}} ...rest },
        ref
    ) => {
		const Element = element

        return (
            <Element ref={ref} className={clsx(className)} {...rest}{{#if children}}>
                {children}
            </Element>{{ else }} />{{/if}}
        )
    }
)
