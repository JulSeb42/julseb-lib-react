/*=============================================== {{ pascalCase name }} tests ===============================================*/

import { {{ pascalCase name }}, LIB_TOKENS } from "../../.."

describe("<{{ pascalCase name }} />", () => {
    it("renders <{{ pascalCase name }} /> component", () => {
        cy.mount(<{{ pascalCase name }} data-testid="testid" className="className"{{#if children}}>Hello</{{ pascalCase name }}>{{else}} />{{/if}})
        cy.dataTest()
            .should("exist")
            .should("have.class", "className")
        
        // ? check all default props
    })
})
