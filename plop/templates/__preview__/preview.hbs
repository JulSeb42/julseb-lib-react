/*=============================================== {{ pascalCase name }}Preview ===============================================*/

import { {{ pascalCase name }} } from "../../../"
import type {  } from "../../../types"
import type { {{ pascalCase name }}Props } from "../../../types/components-props"
import type { ComponentPreview } from "../../../../data/components"

export const {{ camelCase name }}Preview: ComponentPreview<{{ pascalCase name }}Props> = {
    name: "{{ pascalCase name }}",
    component: {{ pascalCase name }},
    category: "{{ category }}",
    imports: ["{{ pascalCase name }}"],
    typeImports: ["{{ pascalCase name }}Props"],
    extends: ["HTML{{ pascalCase attribute }}Element"],
    previews: [
        { previewTitle: "Default", props: {  } },
        { previewTitle: "Default", demo: <{{ pascalCase name }}Preview /> },
    ],
}

function {{ pascalCase name }}Preview() {
    return (
        <{{ pascalCase name }}>
            
        </{{ pascalCase name }}>
    )
}
